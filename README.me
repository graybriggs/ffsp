This project uses the legacy fixed function OpenGL pipeline.
Why? Call it an exercise. FF OpenGL is also very convenient way to knock up a 3D project quickly.
The "3D programming fundamentals" remain regardless of the API.

Dependencies:
SDL2 2.30.0
OpenGL 2.1